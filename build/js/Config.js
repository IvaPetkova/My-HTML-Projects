'use strict';

// External dependencies:

if (typeof $ === 'undefined') console.error('#App - missing required library: jQuery');
if (typeof _ === 'undefined') console.error('#App - missing required library: Underscore.js');

// require('./utils/helpers');
var App = require('./App');

// Main ------------------------------------------------------